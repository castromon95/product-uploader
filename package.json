{
  "name": "product-uploader",
  "description": "Simple product uploader monorepo sample",
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run start:dev -w server",
    "dev": "run-p dev:client dev:server"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
